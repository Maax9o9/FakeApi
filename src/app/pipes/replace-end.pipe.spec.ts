import { ReplaceEndPipe } from './replace-end.pipe';

describe('ReplaceEndPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceEndPipe();
    expect(pipe).toBeTruthy();
  });
});
